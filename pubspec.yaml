name: uigitdev_stream_holder
description: Stream and data holder class, which helps to ensure reusability and reduce the number of lines of code.
version: 1.1.0
homepage: https://github.com/uigitdev/stream_holder

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0